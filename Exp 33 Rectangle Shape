import cv2
import numpy as np

h = int(input("Enter image height: "))
w = int(input("Enter image width: "))

img = np.ones((h,w,3),dtype="uint8")*255

cv2.rectangle(img,(50,100),(400,200),(255,0,0),3)

cv2.imshow("Rectangle Shape",img)

cv2.waitKey(0)
cv2.destroyAllWindows()
